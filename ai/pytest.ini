[pytest]
asyncio_mode = auto
testpaths = tests
# tests/evaluation 은 ragas/datasets 등 별도 의존성이 필요한 수동 오프라인 평가 스크립트라 기본 수집에서 제외
addopts = --ignore=tests/evaluation
markers =
    integration: 실제 LLM 호출 포함 통합 테스트 (CI 제외)
